// Baseline: no top-level symbols. The Slice View e2e test overwrites this
// file with a real `writeRecord` function in the head-side pass so the diff
// sees an `added` Symbol that participates in the 3-layer chain.
